# Server Configuration
PORT=8000
ENVIRONMENT=production

# Security & CORS
# Use * for open access, or specify exact domains like https://instadl.example.com
CORS_ORIGINS=*

# Optional API Keys or Secrets (if needed in future)
# SECRET_KEY=your_super_secret_key_here
