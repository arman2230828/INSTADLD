# Set allowed origins for CORS. Use comma-separated values.
# Use * for open access, or specify exact domains like https://instadl.example.com
CORS_ORIGINS=*

# Add any additional secrets or config below
# SECRET_KEY=your_secret_key
# API_TOKEN=your_api_token
